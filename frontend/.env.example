# URL used by Next.js server components to call the Django API during local development.
BACKEND_API_URL=http://localhost:8000

# URL used by client-side browser code. It must be publicly reachable from the browser.
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000
